; PHP upload limits for Task Fiend.
; These must match (or exceed) MAX_FILE_SIZE in .env.
; Used by the PHP built-in server (php artisan serve) and Apache mod_php.
; For PHP-FPM / Nginx, set these values in the FPM pool configuration instead.
upload_max_filesize = 22M
post_max_size = 24M
